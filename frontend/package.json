{
  "name": "pixhood-frontend",
  "version": "1.0.0",
  "description": "Pixhood frontend - Geo-anchored collaborative pixel art",
  "scripts": {
    "dev": "serve -l 3000",
    "build": "node build.js",
    "preview": "serve -l 3000 dist",
    "deploy": "npm run build && wrangler pages deploy",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "serve": "^14.0.0",
    "sharp": "^0.33.0"
  }
}